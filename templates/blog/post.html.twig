{% extends 'base.html.twig' %}

{% block title %}Hello BlogController!{% endblock %}

{% block body %}

<div class="container mt-5 centralcontent">

    <div class="d-flex justify-content-center">

        <div class="text-center imgpostint"><img class="img-fluid" src={{ post.lienIMG }} />
            <div>
                <h1 class="h1post">{{ post.title }}</h1>
                <p>{{ post.text }}</p>
            </div>
        </div>

    </div>
<hr>
    <div class="flex-row">
        <div class="text-center">

            <h2 class="h1post">Commentaires</h2>

            {% for comment in post.comments %}

            <div class="card flex-row article">
                <div class="card-body">
                    <h4 class="card-title h5 h4-sm">{{ comment.username }}</h4>
                    <p class="card-text">{{ comment.text }}</p>
                    <p class="card-text">écrit le {{ comment.dateCrea|date("d/m/Y") }}</p>
                </div>
            </div>


            {% endfor %}

        </div>
    </div>
<hr>
    <div class="row">
        <div class="text-center">

            <h2 class="h1post">Ajouter un commentaire</h2>

            {{ form(form) }}

        </div>
    </div>
</div>


{% endblock %}